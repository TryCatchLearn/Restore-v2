services:
  sql:
    image: postgres:15
    environment:
      POSTGRES_PASSWORD: "Password@1"
    ports:
      - "5432:5432"
    volumes:
      - pg-data:/var/lib/postgresql/data
volumes:
  pg-data:
